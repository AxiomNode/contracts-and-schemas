openapi: 3.1.0
info:
  title: Internal Microservice Quizz API
  version: 1.0.0
  description: Contrato interno consumido por bff-mobile y bff-backoffice.
servers:
  - url: http://microservice-quizz:7100
paths:
  /health:
    get:
      summary: Health check
      responses:
        "200":
          description: OK
  /games/models/random:
    get:
      summary: Obtiene un quiz aleatorio
      parameters:
        - in: query
          name: language
          schema:
            type: string
            example: es
        - in: query
          name: categoryId
          schema:
            type: integer
      responses:
        "200":
          description: Modelo de quiz
        "404":
          description: Sin modelos disponibles
